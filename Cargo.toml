[package]
name = "leo"
version = "0.1.0"
edition = "2021"

[dependencies]
inkwell = { version = "0.5", features = ["llvm14-0"] }

[profile.release]
strip = true
opt-level = 3
lto = true
codegen-units = 1
